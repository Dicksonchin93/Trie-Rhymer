//SET DEBUG TO 1 to view intermediate outputs
#define DEBUG 0
//SET SYL TO 1 to use syllable preprocessing, set 0 to iterate through each and every character
#define SYL 0
//SET RAND to 1 to random return multiple outputs
#define RAND 1
